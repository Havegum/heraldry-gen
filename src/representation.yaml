# From generator:
Shield:
  division:
    type: string
    props?: Object
  fields: # An array of fields
    - symbol: string
      color: Tincture
      background: Tincture

# In renderer:
Shield:
  shape:
    # Parameters to the shape of the shield
    # These global params in turn affect how the divisions are drawn
    # The symbols are placed based on local logic in the division components.
    alpha: number
    beta: number
    theta: number
  division: # Same as above
  fields: # Same as above
